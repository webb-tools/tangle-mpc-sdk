interface Loaded {
    format: 'commonjs' | 'module';
    shortCircuit?: boolean;
    source: string;
}
type NexLoad = (url: string, context: Record<string, unknown>) => Promise<Loaded>;
/**
 * Load all TypeScript files, compile via tsc on-the-fly
 **/
export declare function load(url: string, context: Record<string, unknown>, nextLoad: NexLoad): Promise<Loaded>;
export {};
